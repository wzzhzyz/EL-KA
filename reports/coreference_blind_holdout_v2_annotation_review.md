# Blind Holdout v2 标注复核

全体 32 条已完成一次单人复核（`single-review limitation`）。复核范围包括 mention 偏移、KB ID、集合 gold、NIL 原因和独立性；多协调组、跨句、复杂 NIL 作为重点复核项。详细 machine-readable 标注位于 `data/eval/coreference_blind_holdout_v2.json` 的样本元数据与 `expected_coreferences`。
