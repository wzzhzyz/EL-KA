# requirements.txt - 稳定版（GPU PyTorch + CPU FAISS）
# ============================================================
# Web 框架
# ============================================================
fastapi==0.115.0
uvicorn[standard]==0.30.0


# ============================================================
# 向量模型与检索 (PyTorch GPU + FAISS CPU)
# ============================================================
torch==2.6.0
torchvision==0.21.0
transformers==5.13.0
sentence-transformers==5.6.0
huggingface-hub==1.22.0
faiss-cpu==1.8.0  # 使用 CPU 版本，不影响 GPU 加速
accelerate==1.14.0
datasets==2.18.0              # ← 新增：HuggingFace 数据集库，用于训练数据加载
tokenizers==0.22.2           # ← 新增：Tokenizer 依赖

# ============================================================
# 数据处理
# ============================================================
numpy==1.24.0
pandas==2.0.0

# ============================================================
# 数据库与配置
# ============================================================
sqlalchemy==2.0.0
pyyaml==6.0
python-dotenv==1.0.0

# ============================================================
# HTTP 请求 (LLM 兜底可选)
# ============================================================
requests==2.31.0

# ============================================================
# 工具
# ============================================================
tqdm==4.66.0